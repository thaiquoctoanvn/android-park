package io.edenx.androidpark.core.designsystem

import androidx.compose.material3.Typography

/**
 * Material 3 defaults for now. The XML side has no custom font either, so
 * there is nothing to mirror yet.
 */
internal val AndroidParkTypography = Typography()
